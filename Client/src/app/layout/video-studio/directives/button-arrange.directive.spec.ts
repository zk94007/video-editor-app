import { ButtonArrangeDirective } from './button-arrange.directive';

describe('ButtonArrangeDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonArrangeDirective();
    expect(directive).toBeTruthy();
  });
});
