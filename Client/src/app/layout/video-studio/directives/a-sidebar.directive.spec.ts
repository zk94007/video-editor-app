import { ASidebarDirective } from './a-sidebar.directive';

describe('ASidebarDirective', () => {
  it('should create an instance', () => {
    const directive = new ASidebarDirective();
    expect(directive).toBeTruthy();
  });
});
