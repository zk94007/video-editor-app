import { BlColorpickerDirective } from './bl-colorpicker.directive';

describe('BlColorpickerDirective', () => {
  it('should create an instance', () => {
    const directive = new BlColorpickerDirective();
    expect(directive).toBeTruthy();
  });
});
