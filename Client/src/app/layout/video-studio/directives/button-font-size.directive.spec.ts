import { ButtonFontSizeDirective } from './button-font-size.directive';

describe('ButtonFontSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonFontSizeDirective();
    expect(directive).toBeTruthy();
  });
});
