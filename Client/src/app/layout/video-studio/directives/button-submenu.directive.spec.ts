import { ButtonSubmenuDirective } from './button-submenu.directive';

describe('ButtonSubmenuDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonSubmenuDirective();
    expect(directive).toBeTruthy();
  });
});
