import { ButtonTextAlignDirective } from './button-text-align.directive';

describe('ButtonTextAlignDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonTextAlignDirective();
    expect(directive).toBeTruthy();
  });
});
