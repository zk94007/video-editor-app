import { ButtonFontFamilyDirective } from './button-font-family.directive';

describe('ButtonFontFamilyDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonFontFamilyDirective();
    expect(directive).toBeTruthy();
  });
});
