import { DivModalDirective } from './div-modal.directive';

describe('DivModalDirective', () => {
  it('should create an instance', () => {
    const directive = new DivModalDirective();
    expect(directive).toBeTruthy();
  });
});
